import http from 'http';import {createApp} from './app';import {hub} from './ws';import {defaultConfig,APP_NAME} from './config';import {getDb,addActivity} from './db';import {generateToken,tokenConfigured} from './auth';import {detectAll} from './detector';import {syncDetected} from './system';import {interruptStale} from './tasks';import {sampleAndRecord} from './metrics';import {enableFileLogging} from './logger';import {initSecuritySchema,audit,type SecurityContext} from './security';import {initOrchestrationSchema} from './orchestration';import {initMcpSchema} from './mcp';import {initMitreSchema} from './mitre';import {initEvidenceSchema} from './evidence';import {initFindingsSchema} from './findings';import {initAiSchema} from './ai';import {initReportingSchema} from './reporting';import {initSafetySchema} from './safety';import {initCapabilitySchema} from './capabilities';import {initAgentRouterSchema} from './agent-router';import {registerConfiguredProviders,syncMcpProvider} from './mcp-sync';import {TaskWorker} from './task-worker';
const BANNER=`\n  █████╗ ██╗     ██████╗ ██╗  ██╗ █████╗ ██╗  ██╗\n ██╔══██╗██║     ██╔══██╗██║  ██║██╔══██╗╚██╗██╔╝\n ███████║██║     ██████╔╝███████║███████║ ╚███╔╝\n ██╔══██║██║     ██╔═══╝ ██╔══██║██╔══██║ ██╔██╗\n ██║  ██║███████╗██║     ██║  ██║██║  ██║██╔╝ ██╗\n ╚═╝  ╚═╝╚══════╝╚═╝     ╚═╝  ╚═╝╚═╝  ╚═╝  ╚═╝\n`;
async function main(){const cfg=defaultConfig();getDb();initSecuritySchema();initScopeSchema();initOrchestrationSchema();initMcpSchema();initMitreSchema();initEvidenceSchema();initFindingsSchema();initAiSchema();initReportingSchema();initSafetySchema();initCapabilitySchema();initAgentRouterSchema();enableFileLogging();const interrupted=interruptStale();const args=process.argv.slice(2);if(args.includes('--help')||args.includes('-h')){console.log(BANNER);console.log(`  ${APP_NAME} — local control plane for your AI agents`);console.log('');console.log('  Usage:');console.log('    npm start                         run the dashboard');console.log('    npm start -- --rotate-token       rotate the access token and print the new one');console.log('');process.exit(0);}if(args.includes('--rotate-token')){const raw=generateToken();console.log(BANNER);console.log(`  ${APP_NAME} access token rotated`);console.log('');console.log(`  New access token : ${raw}`);console.log('   (shown once - write it down. The previous token no longer works;');console.log('    active login sessions are unaffected and registrations are kept.)');console.log('');process.exit(0);}let token:string|null=null;if(!tokenConfigured())token=generateToken();const app=createApp();const server=http.createServer(app);hub.attach(server);const worker=new TaskWorker();server.listen(cfg.port,cfg.host,async()=>{console.log(BANNER);console.log(`  ${APP_NAME} is running`);console.log(`  Dashboard : http://${cfg.host}:${cfg.port}`);if(token){console.log(`  Access token : ${token}`);console.log('   (shown once - write it down. Rotate anytime in Settings.)');}else console.log('  Access token : already configured (login with your token)');console.log('  Auth        : session cookies, local-only bind');console.log('  Security    : policy + approvals + audit ledger enabled');console.log('  Missions    : orchestration API enabled');console.log('  MCP         : governed tool registry enabled');console.log('  Routing     : capability + agent routing enabled');console.log('  Worker      : persistent task worker enabled');if(interrupted)console.log(`  Tasks      : ${interrupted} left in a stale state marked interrupted`);console.log('');addActivity('system','Dashboard started'+(interrupted?` (${interrupted} stale tasks interrupted)`:''));audit('system','security.control-plane.started','security','allow');worker.start();const providers=registerConfiguredProviders();const syncCtx:SecurityContext={actor:'system',role:'admin',risk:'low'};for(const provider of providers){try{const tools=await syncMcpProvider(syncCtx,provider);console.log(`  [mcp] ${provider.name}: discovered ${tools.length} tools`);}catch(e){console.warn(`  [mcp] ${provider.name}: discovery unavailable - ${(e as Error).message}`);audit('system','mcp.provider.sync.failed',provider.id,'deny',{error:String((e as Error).message)});}}sampleAndRecord();setInterval(sampleAndRecord,15000).unref();const runScan=async()=>{try{const candidates=await detectAll();const added=syncDetected(candidates);if(added.length){addActivity('system',`Auto-discovered: ${added.map(a=>a.name).join(', ')}`);console.log(`  [detect] new: ${added.map(a=>a.name).join(', ')}`);}hub.broadcast('detect:done',{count:candidates.length,names:candidates.map(c=>c.name)});}catch(e){console.error('[detect] error:',(e as Error).message);}};await runScan();setInterval(runScan,cfg.detectIntervalSec*1000);});const shutdown=()=>{audit('system','security.control-plane.shutdown','security','allow');worker.stop();console.log('\n  Shutting down…');server.close(()=>process.exit(0));setTimeout(()=>process.exit(0),1500).unref();};process.on('SIGINT',shutdown);process.on('SIGTERM',shutdown);}main().catch(err=>{console.error('Fatal:',err);process.exit(1);});
