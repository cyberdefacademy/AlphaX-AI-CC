import {Router,Request} from 'express';import {type SecurityContext,requirePermission} from '../security';import {normalizeResult,NormalizedResult} from '../result-intelligence';export const router=Router();const ctx=(req:Request):SecurityContext=>({actor:'local-admin',role:'admin',projectId:req.body?.projectId,target:req.body?.target,risk:req.body?.risk??'low',tool:req.body?.tool});router.post('/normalize',(req,res)=>{try{const c=ctx(req);requirePermission(c,'missions.execute');res.json(normalizeResult(c,req.body as NormalizedResult));}catch(e){res.status(400).json({error:String((e as Error).message)});}});
