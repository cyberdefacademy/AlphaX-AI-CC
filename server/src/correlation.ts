import {getDb,nowIso,randomId} from './db';import {audit,type SecurityContext} from './security';
export interface CorrelationInput{taskId:string;missionId:string;evidence:string[];findings:string[];attackTechniques:string[];confidence:number;}
export function initCorrelationSchema(){getDb().exec(`CREATE TABLE IF NOT EXISTS finding_correlations(id TEXT PRIMARY KEY,mission_id TEXT NOT NULL,task_id TEXT NOT NULL,correlation_key TEXT NOT NULL,kind TEXT NOT NULL,confidence REAL NOT NULL,first_seen TEXT NOT NULL,last_seen TEXT NOT NULL,count INTEGER NOT NULL DEFAULT 1,evidence_json TEXT NOT NULL DEFAULT '[]',attack_json TEXT NOT NULL DEFAULT '[]',UNIQUE(mission_id,correlation_key));`)}
const norm=(s:string)=>s.trim().toLowerCase().replace(/\s+/g,' ');const clamp=(n:number)=>Math.max(0,Math.min(1,n));
export function correlate(ctx:SecurityContext,input:CorrelationInput){const db=getDb(),out:any[]=[];for(const finding of input.findings??[]){const key=`finding:${norm(finding)}`;const old:any=db.prepare('SELECT * FROM finding_correlations WHERE mission_id=? AND correlation_key=?').get(input.missionId,key);const confidence=clamp(input.confidence);if(old){db.prepare('UPDATE finding_correlations SET last_seen=?,count=count+1,confidence=?,evidence_json=?,attack_json=? WHERE id=?').run(nowIso(),Math.max(old.confidence,confidence),JSON.stringify(input.evidence??[]),JSON.stringify(input.attackTechniques??[]),old.id);out.push({id:old.id,kind:'existing',confidence:Math.max(old.confidence,confidence)});}else{const id=randomId();db.prepare('INSERT INTO finding_correlations(id,mission_id,task_id,correlation_key,kind,confidence,first_seen,last_seen,evidence_json,attack_json) VALUES(?,?,?,?,?,?,?,?,?,?)').run(id,input.missionId,input.taskId,key,'new',confidence,nowIso(),nowIso(),JSON.stringify(input.evidence??[]),JSON.stringify(input.attackTechniques??[]));out.push({id,kind:'new',confidence});}}audit(ctx.actor,'finding.correlated',input.missionId,'allow',{taskId:input.taskId,correlations:out.length});return out;}
